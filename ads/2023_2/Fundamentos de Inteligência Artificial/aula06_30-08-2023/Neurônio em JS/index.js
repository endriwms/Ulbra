function funcaoRampa(x){
    const s = input * peso;
    return s<0 ? 0 : s <= 1 ?;
};


var data = [
  { entrada: [0, 0], saida: 0} // pelé
  { entrada: [0, 0], saida: 0} // zico
  { entrada: [0, 0], saida: 0} // sena
  { entrada: [0, 0], saida: 0} // pique
    [0,0], // pelé
    [0,1], // Zico
    [1,0], // Senna
    [1,1]  //Piquet
];

const output = [
    0, // futebol
    1 // automobilismo
]